# Base URL of the weather proxy backend. No trailing slash required.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# City loaded on first visit when geolocation is denied or unavailable.
NEXT_PUBLIC_DEFAULT_CITY=Tel Aviv
